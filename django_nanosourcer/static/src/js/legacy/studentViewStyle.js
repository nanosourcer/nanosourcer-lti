$(document).ready(function() {

	// $(".custom-panel-header").on("click", function() {

	// 	var headerState = $(this).attr("class");

	// 	if (headerState == "custom-panel-header-pressed") {

	// 		$(this).attr("class", "custom-panel-header");

	// 	} else {

	// 		$(this).attr("class", "custom-panel-header-pressed");

	// 	}

	// 	var id = $(this).attr('id');
	// 	id = id.replace('header', 'body');

	// 	var state = $("#" + id).attr("class")

	// 	if (state == "custom-panel-body") {

	// 		$("#" + id).attr("class", "custom-panel-body-open");
			
	// 	} else {

	// 		$("#" + id).attr("class", "custom-panel-body");

	// 	}

	// });

	// $("#slider-range-date-filter").slider({

 //      range: true,
 //      min: -50000,
 //      max: 3000,
 //      values: [ -500000, 2016 ],
 //      tap: true,
 //      slide: function( event, ui ) {

 //        $("#dt_end").val(ui.values[1]);
 //        $("#dt_begin").val(ui.values[0])

 //      }

 //    });

    // $(".hamburger-bars-container").on("click", function() {

    // 	if ($("#hamburger-menu").css('display') == 'none') {

    // 		$("#hamburger-menu").css('display', 'initial');

    // 	} else {

    // 		$("#hamburger-menu").css('display', 'none');

    // 	}

    // });

});